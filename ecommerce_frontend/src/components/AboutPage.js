import React from "react";
import Header from "./Header";
const AboutPage = () => (
  <div>
  <Header />
  <br></br>
    <p>In this App MERN stack is use to build a system.</p>
  </div>
);

export default AboutPage;
